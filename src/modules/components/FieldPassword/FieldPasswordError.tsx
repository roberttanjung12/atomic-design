import { FieldPassword } from '@/@dront/components';

const FieldPasswordError = () => {
  return <FieldPassword name="password" label="Password" placeholder="Password" errorMessage="Error message." />;
};

export default FieldPasswordError;
