import FieldPassword from '@dront/ui/FieldPassword';

const FieldPasswordBasic = () => {
  return <FieldPassword name="password" label="Password" placeholder="Password" />;
};

export default FieldPasswordBasic;
