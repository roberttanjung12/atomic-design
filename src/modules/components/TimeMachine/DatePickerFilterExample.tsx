import TimeMachine from '@/@dront/components/TimeMachine';

const DatePickerFilterExample = () => {
  return <TimeMachine variant="date-picker" isFilter filter={{ name: 'dp-query' }} />;
};

export default DatePickerFilterExample;
