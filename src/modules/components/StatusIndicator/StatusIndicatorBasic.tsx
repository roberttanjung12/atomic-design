import { StatusIndicator } from '@/@dront/components';

const StatusBasic = () => {
  return (
    <StatusIndicator
      label="Success"
      slotProps={{
        root: {
          className: 'asdasds'
        }
      }}
    />
  );
};

export default StatusBasic;
