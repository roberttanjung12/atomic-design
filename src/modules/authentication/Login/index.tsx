import LoginForm from './LoginForm';
import LoginLayout from './LoginLayout';

const Login = () => {
  return (
    <LoginLayout>
      <LoginForm />
    </LoginLayout>
  );
};

export default Login;
